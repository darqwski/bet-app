export interface IWithLabel {
    label: string;
    white?: boolean;
    id: string
}

export interface IFormInput {
    label: string;
    name: string;
    white?: boolean;
    type: string;
    disabled?: boolean;
}
